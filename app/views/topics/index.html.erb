<h1>Lista de Tópicos</h1>
<ul>
     <% @topics.each do |topic| %>
          <li><%= link_to topic.title, topic_path(topic)%></li>
          <% end %>
     </ul>

     <%= link_to "Criar Tópico", new_topic_path, class: "btn btn-primary" %>
